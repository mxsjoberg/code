PROGRAM read_and_write;
VAR
    input: string;
BEGIN
    WRITELN('Write something:');    // Write something:
    READLN(input);                  // > hello pascal
    WRITELN(input);                 // hello pascal
END.